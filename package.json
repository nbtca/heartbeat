{
  "name": "heartbeat",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.ts",
    "dev": "wrangler dev --test-scheduled",
    "check": "node scripts/build.ts && wrangler types && tsc",
    "test": "node --test test/*.test.ts",
    "probe": "node probe/main.ts"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "7.0.2",
    "wrangler": "4.131.0"
  }
}
